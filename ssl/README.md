sync-pve-cert.sh 同步证书到pve主机

# 用法
------
不带参数，证书文件与当前脚本同一目录
```shell
# 不带参数，证书文件与当前脚本同一目录
sync-pve-cert.sh
```
带参数，指定证书文件存放目录
```shell
# 带参数，指定证书文件存放目录
sync-pve-cert.sh /path/to/pem
```


# 改版

## 2055/6/19 修改
------
1. 证书存放目录可以通过命令行参数指定，没指定时，默认在当前脚本同目录下。
2. 检测远程服务器是否可以免密登录。
3. 对scp执行结果判定。
